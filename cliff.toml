# ================================
# git-cliff final stable config
# Conventional Commits compatible
# ================================

[changelog]
header = """
# Changelog

所有重要的变更都会记录在此文件中。
格式遵循 [Conventional Commits](https://www.conventionalcommits.org/)。
"""

# 🚀 修改说明：
# 1. {% if version -%} 后面的两个空行确保了版本号上方有一个空行。
# 2. {% for group ... -%} 中的 -% 删除了标签后的换行。
# 3. 显式添加 \n ### {{ group }} 确保标题前有一个空行。
# 4. 内层 {% endfor -%} 和外层 {% endfor -%} 都使用了 -% 来消除循环产生的多余累积换行。
body = """
{% if version %}
## {{ version }}{% if date %} ({{ date }}){% endif %}
{% else -%}
## 未发布
{% endif -%}
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group }}

{% for commit in commits -%}
- {{ commit.message }}
{% endfor -%}
{% endfor -%}
"""

footer = """

---
Book Your ENS Names at ENSBook.
"""

trim = true

# ================================
# Git behavior
# ================================

[git]
conventional_commits = true
filter_unconventional = true
ignore_merge_commits = true

# 只识别 vX.Y.Z 形式的 tag
tag_pattern = "v[0-9]*"

# 提交按时间正序（更符合阅读习惯）
sort_commits = "oldest"

# ================================
# Commit parsers
# ================================

[[commit_parsers]]
message = "^feat"
group = "🚀 Features"

[[commit_parsers]]
message = "^fix"
group = "🐛 Bug Fixes"

[[commit_parsers]]
message = "^perf"
group = "⚡ Performance"

[[commit_parsers]]
message = "^refactor"
group = "🚜 Refactor"

[[commit_parsers]]
message = "^docs"
group = "📚 Documentation"

[[commit_parsers]]
message = "^test"
group = "🧪 Tests"

[[commit_parsers]]
message = "^build"
group = "🏗 Build System"

[[commit_parsers]]
message = "^ci"
group = "🤖 CI"

[[commit_parsers]]
message = "^style"
group = "🎨 Styling"

[[commit_parsers]]
message = "^chore"
group = "⚙️ Miscellaneous Tasks"

[[commit_parsers]]
message = "^revert"
group = "⏪ Reverts"

# ================================
# Breaking changes
# ================================

[[commit_parsers]]
body = "BREAKING CHANGE"
group = "💥 Breaking Changes"

[notes]
keywords = ["BREAKING CHANGE"]
