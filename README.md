# ENSBook

## TODO

- [x] React Query 化
- [x] 引入 Web Worker：将大批量文本解析放入 Web Worker
- [x] 添加溢价期的估价功能
- [x] 添加一个搜索说明，在搜索框的左侧，使用灰色圆形图标按钮。
- [x] 加入官方链接
- [x] 处理单个删除的时候页面跳动的问题
- [x] 增加分类删除功能
- [x] 处理 # 及 @ 后面跟地址的问题
- [x] 顶部固定处理，下拉列表遮挡处理
- [x] 处理断点续传的 bug
- [ ] 在展示大量数据时，引入 react-window 等虚拟滚动技术
- [ ] 实现分页功能
- [ ] 加入 hover 提示功能
- [ ] 支持中英文切换，默认英文
- [ ] 在页面合适位置加入统计信息
- [ ] 状态标签 hover 时显示名称的多个时间节点
- [ ] 检查 linkowner 过程是否会产生错误结果
- [ ] 调整优化溢价期价格显示模式
- [ ] 在表头的序号单元格内显示当前表格中的名称数量
- [ ] 开始注册以后，注册按钮显示当前注册的状态
- [ ] 注册和续费按钮绑定相应的注册或续费处理函数
- [ ] 不同页面之间切换时，先显示骨架屏
- [ ] 筛选按钮悬停显示是不是更好？

## Tables

- 序号：从 1 开始的自然数
- 名称：ens 名称。【如果包装，高亮显示】
- 所有者：显示名称的所有者。【如果与连接的钱包相同，则高亮显示】
- 状态：显示名称的当前状态。
- 相关信息：显示与这个名称有关的链接，暂时留空
- 操作：显示根据名称的状态显示注册或续费按钮 【如果钱包没连接，则为不可用状态】
- 删除：显示多选框用来单个或批量删除【只在 home 页面显示这一列，由于 CollectionDetail.tsx 中显示的是固定数量的名称，因此不需要这一列】

## Styles

## Git commit message

- git commit -m "feat: 在页面中透传筛选计数统计到 NameTable"

## Prompts

### Frequently Used

- eb 项目开发原则按照重要性由高到低依次是：（1）保证安全性，避免逻辑错误和漏洞，确保数据安全和准确；（2）保证健壮性；（3）保持可维护性；（4）提升效率；（5）提高代码可读性；（6）保持代码简洁。其中的后3项为重要性类似，要权衡其优先程度。请按照我们的开发原则，对刚才开发的代码进行检查。
