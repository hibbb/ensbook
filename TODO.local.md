# TODO

## TODO List

- [ ] 市场价值透视：在集合页面集成 Floor Price (地板价) 指示器
- [ ] 市场价值透视：显示每个名称相应的价格
- [ ] 在设置中增加是否显示市场数据的选项，以更好地保护隐私
- [ ] 适配移动端,移动端体验：响应式“卡片视图”适配
- [ ] 在展示大量数据时，引入 react-window 等虚拟滚动技术
- [ ] 如果未来表格数据量超过 10,000 条，这里的实时过滤逻辑可能需要引入 Web Worker 或服务端分页

- [ ] 测试有无出价的情况
- [x] 开发批量更新合约，并更新程序中的调用，测试
- [ ] processmodal 很庞大，文件有无精简或封装的可能，以增加可读性
- [ ] 几个页面重复了许多代码，可否封装
- [ ] 支持用户在注册时自定义域名接收者
- [x] 检查市场数据加载比较慢的问题
- [x] 调整市场栏的样式

## Git commit message

git add .
git commit -m ""

feat（新功能）、fix（修复 bug）、docs（文档）、style（样式/格式，不影响逻辑）、refactor（重构）、perf（性能优化）、test（测试）、build（构建/打包）、ci（持续集成）、chore（杂项/维护）、revert（回滚）

## Prompts

### Temp

### Frequently Used

1. 例行检查优化

eb 项目开发原则按照重要性由高到低依次是：

（1）保证程序安全，避免逻辑错误和漏洞；
（2）保证数据安全和准确，维护数据一致性；
（3）保证健壮性；
（4）保持可维护性；
（5）提升效率；
（6）提高代码可读性；
（7）保持代码简洁；
（8）保持风格样式的统一。

其中的后4项的重要性类似，要权衡其优先程度。请按照我们的开发原则，对最近两天开发的代码进行检查。

2. 开发建议

经过最近一段时间 eb 项目开发，我们又完成了不少工作，包括功能增加和更新，性能优化，ui 和 ux 提升等等。从你的角度考虑，在项目当前的最新状态下，你认为 eb 项目在功能、性能、用户体验等方面还有哪些可以改进和创新的地方？请提出至少两个具体的改进建议和两个具体的创意。

## 特殊指令

```bash
find . -type f \( -name "*.ts" -o -name "*.tsx" -o -name "*.css" -o -name "*.sol" \) -not -path "*/node_modules/*" -not -path "*/.next/*" -exec echo -e "\n\n--- FILE: {} ---\n" \; -exec cat {} \; > code_context.txt
```

## Done

### Main

- [x] React Query 化
- [x] 引入 Web Worker：将大批量文本解析放入 Web Worker
- [x] 数据安全与迁移：本地数据的导入/导出功能
- [x] 智能日历集成：“防断供”提醒系统
- [x] 实现分页功能
- [x] 支持中英文切换，默认英文

### Details

- [x] 添加溢价期的估价功能
- [x] 添加一个搜索说明，在搜索框的左侧，使用灰色圆形图标按钮。
- [x] 加入官方链接
- [x] 处理单个删除的时候页面跳动的问题
- [x] 增加分类删除功能
- [x] 处理 # 及 @ 后面跟地址的问题
- [x] 顶部固定处理，下拉列表遮挡处理
- [x] 处理断点续传的 bug
- [x] 加入 hover 提示功能
- [x] 状态标签 hover 时显示名称的多个时间节点
- [x] 增加自定义备注功能
- [x] 在表头的序号单元格内显示当前表格中的名称数量
- [x] 开始注册以后，注册按钮显示当前注册的状态
- [x] 注册和续费按钮绑定相应的注册或续费处理函数
- [x] 筛选按钮悬停显示是不是更好？
- [x] 改进 wrapped 名称显示样式
- [x] 检查 linkowner 过程是否会产生错误结果
- [x] 不同页面之间切换时，先显示骨架屏
- [x] 在名称的 tooltip 上显示更多详细信息，比如它的长度、namehash、labelhash，并且可复制
- [x] 在所有者 tooltip 上显示两行信息，且均可复制
- [x] 删除按钮轻量化
- [x] 注册和续费 modal 风格扁平化
- [x] 按注册时间排序
- [x] 按长度排序有必要吗？删除
- [x] 仅显示有备注的名称
- [x] 筛选和排序信息保存为用户信息，并加入一键清空视图状态的功能（固定浮动按钮）
- [x] 查看全局可删除的文件和函数，可复用的函数或结构
- [x] 堆栈式插入数据
- [x] 调整优化溢价期价格显示模式
- [x] 信息列表更换为图片 logo
- [x] 增加 Mine 页面
- [x] 删除 # 标识符的用法
- [x] 在名称的 tooltip 上添加三行：标签、名称、点击提示，其中标签和名称后附一个可以用于复制的小图标。
- [x] 在序号列或者其他地方加入标记功能
- [x] 让 ai 更新 readme.md 文件
- [x] 增加一个 Account.tsx 页面，用于查看某个名称或地址的持仓情况
- [x] 设置内容的注册偏好中，可设置默认注册和续费时长，页面显示注册时长或到期时间时，进行实时计算，避免硬编码时间造成的错误
- [x] 设置内容的注册偏好中，可选择使用“注册时间”，可选择（）年（）天，注意必须要大于最小注册天数这个常量
- [x] 设置内容的注册偏好中，可选择使用“到期时间”，注意必须要大于最小注册天数这个常量，然后对注册过程的页面进行相应的改造
- [x] 账户中余额不够时，注册或续费会发生什么？是否需要处理。
- [x] 增加一个账户重置按钮。
- [x] 检查几个 page 页面中是否有些代码是重复并且可以复用的。
- [x] 移除对测试网的支持
- [x] 处理移动端不停弹窗的问题，大概率是 imtoken 本身的问题
- [x] 在账户持仓标题左侧加入一个后退按钮
- [x] 简单“仅显示当前已连接钱包的”逻辑，只有当列表中有其名称时，才显示这个图标
- [x] 需要按所有者筛选吗？如何处理太多所有者的情况？问问 ai。
